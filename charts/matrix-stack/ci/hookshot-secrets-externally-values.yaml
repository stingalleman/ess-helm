# Copyright 2025-2026 Element Creations Ltd
#
# SPDX-License-Identifier: AGPL-3.0-only

# source_fragments: hookshot-additional-secrets-externally.yaml hookshot-ingress.yaml hookshot-minimal.yaml hookshot-secrets-externally.yaml init-secrets-disabled.yaml server-name.yaml
# DO NOT EDIT DIRECTLY. Edit the fragment files to add / modify / remove values

# postgres don't have any required properties to be set and defaults to enabled
deploymentMarkers:
  enabled: false
elementAdmin:
  enabled: false
elementWeb:
  enabled: false
hookshot:
  additional:
    example-value:
      configSecret: "{{ $.Release.Name }}-hookshot-external"
      configSecretKey: config
  appserviceRegistration:
    secret: "{{ $.Release.Name }}-hookshot-external-registration"
    secretKey: registration.yaml
  enabled: true
  ingress:
    host: hookshot.ess.localhost
  passkey:
    secret: "{{ $.Release.Name }}-hookshot-external-passkey"
    secretKey: passfile
initSecrets:
  enabled: false
matrixAuthenticationService:
  enabled: false
matrixRTC:
  enabled: false
serverName: ess.localhost
synapse:
  enabled: false
wellKnownDelegation:
  enabled: false
