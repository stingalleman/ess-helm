# Copyright 2024-2025 New Vector Ltd
# Copyright 2025-2026 Element Creations Ltd
#
# SPDX-License-Identifier: AGPL-3.0-only

# source_fragments: init-secrets-disabled.yaml matrix-rtc-external-livekit-secrets-externally.yaml matrix-rtc-external-livekit.yaml matrix-rtc-minimal.yaml
# DO NOT EDIT DIRECTLY. Edit the fragment files to add / modify / remove values

# postgres don't have any required properties to be set and defaults to enabled
deploymentMarkers:
  enabled: false
elementAdmin:
  enabled: false
elementWeb:
  enabled: false
initSecrets:
  enabled: false
matrixAuthenticationService:
  enabled: false
matrixRTC:
  extraEnv:
    - name: LIVEKIT_URL
      value: wss://demo.livekit.cloud
  ingress:
    host: mrtc.ess.localhost
  livekitAuth:
    keysYaml:
      secret: "{{ $.Release.Name }}-matrix-rtc-external"
      secretKey: keys.yaml
  sfu:
    enabled: false
synapse:
  enabled: false
wellKnownDelegation:
  enabled: false
